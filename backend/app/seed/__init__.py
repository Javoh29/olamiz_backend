"""Идемпотентные сиды справочников (гео, позже категории).

Запуск: `uv run python -m app.seed`. Повторный запуск безопасен —
существующие записи не дублируются (get-or-create по естественному ключу).
"""
